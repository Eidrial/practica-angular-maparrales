import { TraducirRolPipe } from './traducir-rol.pipe';

describe('TraducirRolPipe', () => {
  it('create an instance', () => {
    const pipe = new TraducirRolPipe();
    expect(pipe).toBeTruthy();
  });
});
